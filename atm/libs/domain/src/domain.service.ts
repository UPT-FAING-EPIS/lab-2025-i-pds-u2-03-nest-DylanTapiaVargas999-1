import { Injectable } from '@nestjs/common';

/**
 * Servicio de dominio principal.
 * Este servicio puede ser extendido para incluir la lógica de negocio principal del dominio.
 */
@Injectable()
export class DomainService {}
