import { Injectable } from '@nestjs/common';

/**
 * Servicio de dominio principal para el módulo de pagos.
 * Este servicio puede ser extendido para incluir la lógica de negocio principal relacionada con pagos.
 */
@Injectable()
export class DomainService {}
